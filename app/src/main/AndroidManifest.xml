<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ideasmart.orichiraiya" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/applogo"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".SplashActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ChooseColorActivity"
            android:label="Select the bird color"
            android:parentActivityName=".SplashActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.ideasmart.orichiraiya.SplashActivity" />
        </activity>
        <activity
            android:name=".ChooseBillTypeActivity"
            android:label="Select a beak type"
            >
        </activity>
    </application>

</manifest>
